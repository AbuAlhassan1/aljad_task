import 'package:flutter/material.dart';

class RealEstatePageWrapper extends StatelessWidget {
  const RealEstatePageWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}